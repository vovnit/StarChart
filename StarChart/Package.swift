// swift-tools-version:5.3
import PackageDescription

let package = Package(
    name: "StarChart",
    products: [
        .library(
            name: "StarChart",
            targets: ["StarChart"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "StarChart",
            dependencies: []),
    ]
)
